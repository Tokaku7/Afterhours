# Afterhours 2.0.1 更新公告

2.0.1 主要更新了主题系统与统计页面，原有计时、识别和本地数据功能继续保留。

系统托盘现在使用 Afterhours 专属的“月牙＋四格活动”图标，在浅色和深色任务栏上更容易识别。

正常启动请双击 `Afterhours启动.vbs`；`诊断启动.cmd` 只在启动失败时使用，它会保留错误窗口并提示日志位置。

## 新主题

- 主题调整为「极地雾」「极光薄荷」「烟粉」三套 Atmospheric Frost 色板。
- 热力图、游戏标签、进度条、统计环图和记录色标会使用同一套主题颜色。
- 深色模式改为午夜霜蓝玻璃；深色热力图使用“越亮代表时长越多”的显示方式。
- 主窗口、菜单和卡片提高了玻璃底色覆盖度，在复杂壁纸上更容易阅读。

## 新统计页面

- 顶部直接显示所选时间范围的总时长、日均时长和有记录天数。
- 新增 24 小时时段分布，显示游戏通常集中在哪些时间，并标出最常玩的连续 3 小时。
- 游戏占比使用环图与对齐图例展示，可同时看到时长和百分比。
- 最近记录显示游戏名称、日期、开始时间、结束时间和本次时长。
- 支持最近 7 天、30 天和全部记录切换。

## 原有功能仍然保留

- 前台时长与后台运行时长统计。
- Steam 游戏自动识别，以及 Apex Legends、无限暖暖等特殊进程回退规则。
- 手动添加游戏和删除手动规则。
- 8 周活动热力图、历史翻页、按游戏筛选和悬停查看每日明细。
- 系统托盘退出、桌面层显示、窗口缩放、开机自启和深色模式。
- 锁屏、休眠或桌面不可读时暂停累计。
- 所有数据继续保存在本机，不会上传。

## 如何更新

### 从 1.0 升级

推荐先阅读包内的 [`1.0-数据迁移教程.md`](1.0-数据迁移教程.md)，其中包含备份、复制、验证和失败恢复步骤。

1. 关闭 1.0；如果 2.0.1 已经打开，也请从托盘退出。
2. 备份 1.0 的整个 `data` 文件夹。
3. 解压 2.0.1，但先不要启动。
4. 在 2.0.1 文件夹中新建 `data` 文件夹。
5. 从 1.0 的 `data` 中复制 `activity.json`、`games.json` 和 `preferences.json` 到新版 `data`。
6. 双击 `Afterhours启动.vbs` 启动。

1.0 没有 `sessions.json`，所以每日累计、游戏累计和手动规则能够延续；精确的游戏起止时间只能从 2.0.1 开始记录。

### 从 2.0 升级

1. 从托盘退出 2.0，并备份整个 `data` 文件夹。
2. 解压 2.0.1。
3. 将旧版的 `activity.json`、`games.json`、`preferences.json` 和 `sessions.json` 复制到新版 `data` 文件夹。
4. 双击 `Afterhours启动.vbs` 启动。

不要复制旧的 `runtime.json`、错误日志或 `.bak` 文件。若直接在原文件夹覆盖程序文件，请保留原来的 `data` 文件夹。

如果新旧两个版本都已经产生了新记录，请不要直接覆盖 `activity.json` 或 `sessions.json`，否则会丢失其中一边的数据。请先备份两个 `data` 文件夹再进行合并。

## 已验证

- PowerShell 与 XAML 解析通过。
- 前台、后台、跨午夜、休眠间隔、锁屏和退出保存测试通过。
- 三套主题、深色模式、统计页面、时间范围切换、热力图筛选、悬停明细和演示数据隔离测试通过。
