# Afterhours 更新日志

## 2.0.2 — 2026-09-18

### 修复

- 修复“−”折叠按钮受窗口最小高度限制而无法缩小的问题；折叠后显示 160 像素高的小窗，点击“+”恢复，并保留折叠前由用户调整的宽高。
- 修复右下角拖动区域无法调整页面大小的问题；窗口恢复可缩放状态，完整模式支持 300 × 520 至 540 × 860 的尺寸范围。
- 调整窗口时，字体、按钮、热力格、游戏卡片与统计图表会以同一比例缩放，避免只放大外框、内部内容尺寸不变。
- 最小窗口宽度由 340 像素调整为 300 像素，并收紧热力图卡片宽度，减少窄窗口下网格两侧的空白。
- 托盘菜单继续只保留“退出 Afterhours”，避免有问题的显示/隐藏入口再次出现。

## 2.0.1 — 2026-09-17

### 改进

- 新增「暮光紫」主题，以低饱和灰紫玻璃底搭配紫藤色热力图、游戏标签、进度条和统计图表，并兼容深色模式。
- 正常启动入口统一为 `Afterhours启动.vbs`；原 `启动看板.cmd` 改名为 `诊断启动.cmd`，仅用于显示启动错误和日志位置。
- 托盘菜单移除存在状态异常的“显示 / 隐藏看板”和双击显示行为，仅保留可靠的“退出 Afterhours”。
- 新增 Afterhours 专属托盘图标，以月牙与四格活动数据表达夜间游戏统计，并提供 16 至 256 像素的 Windows 图标尺寸。
- 主界面、菜单和卡片的玻璃底色提高到约 85% 不透明度，在深色或复杂壁纸上更清晰易读。
- 主题重做为「Atmospheric Frost」三套：极地雾（灰蓝）、极光薄荷（青绿）、烟粉（玫瑰灰）。每套由玻璃底色、选中控件色、三级图表色和文字色组成，热力图、进度条、时段柱、环图与记录色标统一取自同一套色板；深色模式使用午夜霜蓝的玻璃底色，热力图在深色底上改为「越亮越多」的梯度。旧的主题名会自动回退到极光薄荷。
- 统计页面重新设计：顶部三格概览（总时长 / 日均 / 游玩天数）；新增 24 小时「每日时段分布」并自动标出最常玩的 3 小时；每日时长已由首页热力图承担，统计页不再重复；游戏占比为环图 + 图例卡片，配色随主题变化；最近记录为带游戏色标的卡片列表。演示模式补充了示例会话数据。

### 兼容与保留

- 保留 7 天、30 天和全部记录筛选，统计结果继续读取原有 `activity.json` 与 `sessions.json`。
- 保留前台与运行时长统计、Steam 自动识别、Apex Legends 与无限暖暖回退规则，以及手动添加和删除程序规则。
- 保留热力图翻页、按游戏筛选、悬停查看当日明细、桌面层显示、系统托盘、开机自启和独立演示模式。
- 2.0 已有本地数据无需转换；更新程序文件时不要删除或覆盖自己的 `data` 文件夹。

## 2.0 — 2026-09-17

这是从 1.0 升级而来的功能与稳定性更新。

### 新增

- 新增系统托盘图标，可显示、隐藏或退出 Afterhours。
- 新增可视化统计页面，可切换最近 7 天、30 天或全部记录。
- 新增游戏时长占比环图、每日趋势、常玩时段和最近游戏记录。
- 新增精确会话记录，保存游戏日期、开始时间、结束时间与本次时长。
- 新增主题颜色选择和深色模式；热力图、游戏标签与统计图表会随主题变化。
- 新增开机自启开关。
- 新增手动游戏管理，可删除误添加或不再需要的程序规则。
- 新增窗口尺寸拖动，可按需要缩小或放大看板。
- 新增 Apex Legends 与无限暖暖的常见游戏进程识别规则。

### 改进

- 默认窗口调整得更紧凑，统计页面保持 370 × 610 的小组件比例。
- 启动改为隐藏方式，不再长期占用任务栏中的 PowerShell 窗口。
- 看板保持在桌面层，不遮挡游戏及其他普通窗口。
- 重做透明毛玻璃界面、下拉菜单、交互悬停和文字对比度。
- 热力图改为方形比例，并支持按单个游戏筛选。
- 鼠标悬停热力图日期时显示当日游戏、前台时长和运行时长。
- 今日游戏明细改为带比例条的可视化列表。
- 演示数据移入设置菜单，且与真实统计数据隔离。
- Steam 游戏会在启动时读取本机游戏库并生成识别规则。
- 锁屏、休眠、桌面不可读或采样中断超过 5 秒时暂停累计，减少虚假时长。

### 修复

- 修复分享包引用作者电脑 `E:` 盘路径，导致没有 E 盘的用户启动失败的问题。
- 修复无效或已经不存在的 Steam 游戏库路径导致启动中断的问题。
- 修复圆角窗口四角出现不透明黑色或灰色直角底色的问题。
- 修复下拉菜单出现黑边的问题。
- 修复部分 Steam 游戏只识别启动器、无法识别实际游戏进程的问题。
- 修复分享包可能混入运行数据、日志或本机路径的问题。

### 从 1.0 升级

- 2.0 不提供长期存在的数据导入按钮。
- 如果需要保留 1.0 历史，请在第一次启动新版前阅读包内的 `数据迁移教程.txt`。
- 1.0 的 `activity.json` 可以延续每日累计数据。
- 1.0 没有保存具体会话起止时间，因此精确时间轴只能从 2.0 开始记录。
- 如果 1.0 和 2.0 都已经产生新记录，请先备份两个版本的 `data` 文件夹，不要直接互相覆盖。

### 统计口径

- Afterhours 只统计程序运行期间识别到的游戏活动，不会回填安装前或未运行期间的历史。
- “前台时长”表示游戏窗口位于前台的时间；“运行时长”表示游戏进程存在的时间。
- 所有记录保存在本机 `data` 文件夹，不上传到服务器。

## 1.0

- 初始公开版本。
- 支持本地识别游戏进程并累计前台与运行时长。
- 支持活动热力图、今日游戏列表、手动添加游戏和演示数据。
- 支持桌面悬浮显示与本地 JSON 数据保存。
